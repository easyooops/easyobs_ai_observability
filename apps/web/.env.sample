NEXT_PUBLIC_API_URL=http://127.0.0.1:8787

# Match EASYOBS_SEED_MOCK_DATA on the API so the web app defaults (e.g. Judge Models → Mock) stay aligned with demo seeding.
# NEXT_PUBLIC_EASYOBS_SEED_MOCK_DATA=true
